# 性能修复报告

## 热点定位

用 `performance.now()` 对 3600 帧（等价 60 秒模拟）分模块计时，乱斗模式结果如下。

修复前：
- `Game.draw`：607.6ms
- `updateHud`：129.7ms
- `updateAITanks`：63.4ms
- `updateBullets`：28.9ms
- 总计：741.9ms

修复后：
- `Game.draw`：202.3ms
- `updateAITanks`：56.0ms
- `updateBullets`：18.5ms
- `updateHud`：4.4ms
- 总计：295.8ms

最耗时的三个模块：Canvas 整体绘制、AI 更新、子弹更新。

## 根因

1. 每帧整屏重绘地图，且每帧重建 HUD DOM，造成持续开销。
2. AI 每帧重算目标、队友、视野和可移动方向。
3. 经典/生存模式下死亡坦克不会从数组移除，长时间运行后数组逐渐变大。
4. 乱斗模式击杀敌人时访问不存在的 `this.game.particles` 会抛出异常，直接中断 `requestAnimationFrame` 主循环，表现为运行几秒后卡死/帧率骤降。

## 修改

- 地图静态背景改为离屏 Canvas 缓存，只有砖墙、钢墙、基地变化时才重绘；水波动画单独叠加。
- HUD 从每帧更新降为 10Hz 更新。
- AI 的目标、队友、视野、移动检查改为短缓存，不再每帧重算。
- 非乱斗模式每秒清理死亡坦克，避免实体数组无限增长。
- 补上 `Game.particles = ParticleSystem`，修复乱斗击杀时的运行时异常。

## 验证

- 乱斗模式 6 辆坦克手动驱动 3600 帧，无异常。
- HUD 调用次数从 3600 次降到 514 次。
- 总耗时下降约 60%。
- 经典模式死亡坦克清理验证通过。
- 使用真实 `requestAnimationFrame` 连续采样 1800 帧：平均帧间隔 16.67ms，最大 16.8ms，没有超过 50ms 的掉帧。
- 受无头环境限制，无法输出 DevTools Performance 面板截图，以上使用 `performance.now()` 做等效计时。

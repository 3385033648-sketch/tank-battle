# 修复说明

## 乱斗模式 3v3

根因：乱斗地图下方两个 AI 出生点压到了 2x2 钢墙上，`spawnTank` 静默返回 `null`，导致蓝方和红方各少生成 1 辆坦克，实际变成 4 辆。

修改：
- 调整乱斗地图下方出生点，避开钢墙。
- 乱斗初始化改为按 `teamSize = 3` 循环生成，蓝方为玩家 + 2 AI，红方为 3 AI。
- AI 生成带安全落点回退，不会因出生点重叠而静默少人。
- 复活时按坦克实际尺寸检查出生点，玩家 28px 不会被当成 26px 处理。

## 坦克卡墙

根因：坦克出生、换图或复活时可能落到墙体上；旧移动逻辑只会在碰撞时拒绝移动，不会把已经嵌入墙体的坦克推回空地，于是所有方向键都失效。

修改：
- 新增 `canPlace` 和 `resolveCollision`：一旦坦克矩形与墙体重叠，自动向最近的合法位置回退。
- 新增卡住计数：连续 15 次尝试移动但位置不变时再次触发碰撞修正。
- 玩家出生、AI 生成、关卡/波次换图、铁锹道具把砖墙变成钢墙时都会校验并修正位置。

## 验证结果

- 乱斗模式进入后：蓝方 3 辆，红方 3 辆，共 6 辆，全部存活且不压墙。
- 经典模式第 1/3/5 关：玩家出生点不压墙。
- 把玩家或敌人坦克强制放入墙内后，下一次移动会自动弹到合法空地。
- 复活逻辑按坦克实际尺寸检查，玩家复活后不压墙。
